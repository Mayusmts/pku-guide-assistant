# 技术说明

「燕园手续导航」的实现细节、数据处理方针与取舍记录。作品介绍见 [README.md](README.md)，日文原版见 [README.ja.md](README.ja.md)。

## 为什么做成清单

原文是 note 上的目录式长文，读者需要自己筛选，其中两件事特别不容易看出来。

1. **答案随身份变化** —— 文章自己就写明「非学位生、学位生、预科生、本科生、研究生的日程可能完全不同」。
2. **手续之间藏着依赖顺序** —— 申请居留许可期间护照要上交，所以 SIM 卡和银行开户必须赶在它之前办完。而银行开户又需要住宿登记表和中国的电话号码。这个顺序只作为正文里的一句话存在。

所以改成：选完「签证 / 住处 / 项目」三个条件后，不相关的手续消失，每项手续下面列出「先に必要なこと」（必须先办完的事）并附上理由。

依赖图的形状会随住处变化（住宿舍的话前台代办住宿登记，所以不依赖 SIM 卡；租房需要自己在线登记，因此需要中国的电话号码）。

## 构建

```
python build_data.py    # api_export/<id>.json 的正文 HTML -> app/data/guide.json
python check_data.py    # 检查引用完整性与依赖是否成环
python build_app.py     # template.html + retrieval.js.part + data -> app/index.html
python build_docs.py    # 代理服务端查表用的 server/docs.json（使用问答功能时）
python proto_retrieval.py   # 检索方式的验证（输出 recall@k）
```

### 正文取自 HTML，不取 text

`materials/inventory.json` 里的 `text` 是把正文按行压平的结果，note 的粗体会变成独立的一行。举例来说，「本ガイドを参照したことにより発生した被害や損害等について、本ガイド作成者は一切の責任を負いません」这一句会被拆成 4 行。这种状态下无法区分段落和列表项，把短行当作列表项处理，一句话就会变成 4 个条目。

所以改为解析 `materials/api_export/<id>.json` 里的 `data.body`（原始 HTML），保留段落、列表、标题的结构再取出来。正文中出现的标签只有 `p / li / ul / ol / h2 / h3 / strong / em / a / br / hr / figure / figcaption / img / blockquote / table-of-contents`，这个范围用 `html.parser` 做一个小转换器就够了。

- `h2` 对应 `■` 标题，`h3` 对应 `▶` 标题（从属关系用左侧竖线表示）
- `strong` 是作者的强调意图，予以保留（全文共 968 处）
- 用 `<br>` 挤在同一段里的 `⚠️` `⭐️` 一句话提示，切分成独立的注记块（41 处）
- 课程时间表在原文里是用 note 的数学公式功能（`\begin{array}`）排的，为了不变成裸 LaTeX，还原成矩阵后重新组成表格（文字和排列不变）

内联元素只保留 `strong / em / br / a[http(s)]`，文本在生成时先做转义再嵌入。页面侧用 `innerHTML` 注入。

### figure 不一定是图片的容器

note 不只用 `<figure>` 装图片，附件、外部链接卡片、引用（要点框）也用它装。138 个里有 39 个不含 `<img>`。一律当作图片处理的话，链接和附件的内容会消失，只剩下一个「有图」的空框（实际发生过一次）。`classify_figure` 会看内容再分流：

| figure 的内容 | 输出 | 数量 |
|---|---|---|
| 指向 `note.com/api/v2/attachments/download/` 的链接 | 附件（名称・大小・链接） | 10 |
| `<img src>` | 图片，或图的占位框与说明文字 | 98 |
| `<blockquote>` | 原样保留内部的引用 | 3 |
| 外部链接＋`<strong>`＋`<em>` | 链接卡片（标题・域名） | 25 |
| 空 | 丢弃 | 2 |

引用里有时会含链接，所以要比链接卡片先判定（顺序反过来的话，指向「はじめに必ずお読みください」的引导会变成卡片）。

### 键：值形式的列表排成两列

原文用「时期：内容」的列表来写月度安排、办事窗口和费用。用圆点平铺的话，键和值会混进正文里，所以只把过半数呈「键：值」形态的列表改成定义列表（116 处）。粗体的位置有三种写法，用 `KV_FORMS` 吸收。只改其中一两项反而更难读，因此限定为 **2 项以上且 6 成以上**符合时才转换。

另外，整段只有一行短粗体的情况，是原文在 h3 之下又用了一级小标题，因此按标题处理（139 处）。为避免误伤较长的强调句，限定在 42 字以内。

### 图片转成 webp 作为附属文件分发

`assets` capability 一旦声明，页面就会变成组织内可见、无法公开分享，因此不用。改为只把需要的图片转成 webp，通过 Artifact 的 `files` 一起分发并用相对路径引用（同源，所以不受 CSP 限制）。年度日程图保持 1536×1024，webp quality 82，249KB → 155KB。加载失败时用 `onerror` 换成只剩说明文字的占位框。

要输出的图片列在 `IMAGE_EMBED`，不输出的列在 `IMAGE_DROP`，都按 URL 登记。

`app/index.html` 是生成物。要编辑的是 `app/template.html` 和 `app/data/tasks.json`。

数据不通过 fetch 加载，而是嵌入 `<script type="application/json">`。Artifact 的 CSP 会静默丢弃非同源的 fetch，嵌入的方式不存在加载失败的余地。

## 文件

| 路径 | 作用 |
|---|---|
| `app/template.html` | 页面本体（手写）。用于注入 `__RETRIEVAL_JS__` / `__GUIDE_JSON__` / `__TASKS_JSON__` / `__SYN_JSON__` |
| `app/retrieval.js.part` | **手写**。2-gram BM25 检索模块（由 `build_app.py` 嵌入） |
| `app/data/synonyms.json` | **手写**。在提问的说法与资料的写法之间搭桥 |
| `server/local.py` | **手写**。本地试用的一体化服务端（仅用 Python 标准库） |
| `server/index.mjs` | **手写**。部署用的代理服务端（FC 3.0 Web 函数） |
| `server/rules.txt` | **手写**。给模型的指令文。Node 版与 Python 版共用 |
| `server/docs.json` | 生成物。代理侧按 id 查正文的表（588 条） |
| `proto_retrieval.py` | 检索方式的验证。用人工标注正解的 12 道题测 recall@k |
| `app/data/tasks.json` | **手写**。27 项手续・21 条依赖・36 个术语。从正文抽取的携带物品、费用、注意事项 |
| `app/img/*.webp` | 生成物。通过 `Artifact` 的 `files` 一起分发的 5 张图（年度日程、X1 / X2 签证与居留许可的样张、X1・X2 与居留许可的关系） |
| `app/data/guide.json` | 生成物。45 篇文章 525 个小节，以段落・列表・定义列表・注记・表格・附件・链接卡片为单位保存 |
| `materials/api_export/<id>.json` | note API 的抓取结果。`data.body` 是正文 HTML（正文的来源） |
| `materials/inventory.json` | 标题・URL・发布日・更新日的来源（不使用 `text`） |
| `build_data.py` | 把正文 HTML 连同结构一起转换，并分配分类（12 类）与适用条件标签 |
| `check_data.py` | 检查 sources 是否为真实存在的文章 id、requires 是否成环等 |

## 资料的处理方针

- 正文按刊载时的原文原样收录。不做摘要，不做改写。
- `tasks.json` 里的携带物品、费用、注意事项，只依据文章正文中写明的内容组建。正文里没有的日期、金额、条件一律不添加。
- 每项手续必须带有引用文章（`sources`）。`check_data.py` 会把空的 `sources` 判为错误。
- 文章之间说法不一致的地方不擅自裁决，作为 `discrepancy` 同时呈现两边。

## 不放通往 note 的入口

本页不跳转到 note。`build_data.py` 中丢弃了以下内容。

- **每篇文章的原文 URL**（不输出 `url` 字段）。文章末尾原有的「在 note 打开原文」和「发布／更新」两行也从页面上删除。最后更新日保留在文章标题的右侧。
- **指向 note 页面的链接** 49 条（`strip_note_links`）。去掉链接保留文字，但如果链接文字本身就是 URL，留下的只是一个裸 URL，因此直接删除。
- **note 版的目录**（`nafe1e8c9b900`）。其内容是全部 45 篇的 note 链接，而本页有自己的分类导航，因此用 `EXCLUDE` 排除。文章数为 45 篇。
- **note 平台上的运营信息**（`DROP_TEXT`）。「✉️ 提问箱」「📨 联系创作者」以及「更新状况等」（PDF 版／note 版的制作履历）。指向这些内容的「请从下方联系」一句因为失去了收件对象，也一并删除。

**附件的下载 URL（`note.com/api/v2/attachments/download/`）予以保留。** 这是文件的获取而非跳转到 note 页面，去掉的话 10 个 PDF・DOC 就只剩名字、无法打开。如果也要去掉，需要同时去掉 `NOTE_PAGE_LINK` 的排除条件与 `classify_figure` 的附件判定。

来源署名予以保留（`sourceName` / `sourceBy`）。只是文字，不是链接。

### 排除要逐项判定

把 `DROP_TEXT` 按块（block）整体套用的话，会连同排在同一个列表里的免责声明一起删掉（「はじめに必ずお読みください」的 5 条免责事项与联系方式引导被放在同一个 `ul` 里，实际发生过一次全部消失）。`clean_blocks` 中只对列表做逐项判定。

## 问答（用自然语言检索）

### 为什么必须"先检索"

能传给模型的输入上限是 64KiB（日文约 21,000 字）。而资料有 14 万字，**不可能全部传过去**。这不是优化，而是前提条件：必须先检索，只挑出相关的小节。

### 检索用 2-gram BM25（`app/retrieval.js.part`）

日语没有词的分隔，所以不依赖词典，按 2 个字符为单位建索引。文档为小节 525 + 手续 27 + 术语 36，共 588 条。在此之上加了两项。

1. **同义词**（`app/data/synonyms.json`，手写）—— 「ケータイ」与资料里的「携帯」在 2-gram 上完全不重叠。实测中「ケータイの料金は？」把出租车宰客和 ATM 吞卡排在了前面。做法是在提问侧补上资料的说法来搭桥。
2. **内容词的完全匹配**（`W_EXACT`）—— 「中関新園」「居留許可」这类固有名词切成 2-gram 后会散掉。实测中「中関新園に住みます」的第 1 名是铁道地图，第 2 名是中关园食堂。对原词原样出现的文档单独加权提升。

也加重了对长文的惩罚（`B = 0.82`）。因为 2.6 万字的「9.1 其他信息・注意事项」不管问什么都会排到前面。

`proto_retrieval.py` 是同一套公式的验证版，用人工标注正解的 12 道题测得 **recall@8 = 12/12、recall@12 = 12/12**（其中 7 题排第 1）。前 12 条的总量为 2,566〜6,401 字，相对 64KiB 的限制有充分余量。

样本小且正解由自己标注，所以这只能证明"方式成立"，**不等于"质量已验证"**。

### 不让模型编造引用

让模型用 `[S1]` 的形式写出依据。这个编号**只在那次提问所传入的资料上存在**，所以模型无法编造引用目标。页面侧先把编号映射回真实的小节再转成跳转目标，超出范围的编号显示为虚线的无效标记（不静默放过）。

### API 密钥不能放在页面里

页面会整份落到浏览器上，密钥一旦嵌进去谁都能读，也就会被别人直接拿去用。另外 Artifact 的 CSP 会静默丢弃外部 fetch，就算做好暴露密钥的心理准备，在 Artifact 上也连不通。所以必须隔一层持有密钥的东西。而这一层**不一定要上云**。

| | 用途 | 密钥的存放位置 |
|---|---|---|
| `server/local.py` | 本地试用。仅 Python 标准库，无需 Node | 环境变量（仅本进程） |
| `server/index.mjs` | 给大家用。FC 3.0 的 **Web 函数** | FC 的环境变量 |

两者读取同一份 `rules.txt`（指令文）与 `docs.json`（资料）。

`local.py` 连页面也由同一个服务端提供，因此是同源的，CORS 和 CSP 都不再相关。`app/index.html` 分发时 `AI_ENDPOINT` 保持为空，由 `local.py` 在返回时注入 `/ask`（不必生成两种产物）。

`AI_ENDPOINT` 为空时，问答标签页只靠检索也能工作。检索层在没有接入点的情况下所有人都能用，仅用自然语言查资料的话这样就够了。

本地的 `local.py` 已经实际接通上游（qwen3.7-plus），跑通了往返与 SSE 流式返回（2026-09-17）。跑通的是链路，答案内容的评价另算（见「未收录・未实现」）。

部署步骤、滥用对策、实测范围见 [`server/README.md`](server/README.md)。资料更新后要用 `python build_docs.py` 重新生成代理侧的资料表。

## 布局方针

每个标签页的宽度和排法都不同，不统一套用多栏。

| 标签页 | 宽度 | 排法 | 理由 |
|---|---|---|---|
| 手续 | 全宽 1120px | 概览用横向网格，阶段用纵向堆叠的块 | **手续的内容就是顺序**。把阶段左右分栏会让阅读顺序变成 Z 字形，前后关系就被破坏了 |
| 资料 | 880px | 分类标签筛选＋单栏 | 正文是读物，控制在 66ch 以内。12 个分类纵向堆叠的话，靠后的分类就太远了 |
| 术语表 | 全宽 1120px | `auto-fill` 的卡片网格 | 词条短且没有顺序，铺满是最自然的 |
| 问答 | 880px | 单栏 | 这里是读答案和引用来源的地方 |

标签页共 4 个：手续 / 资料 / 术语表 / 问答。「关于本页」标签按作者的指示删除了。非官方这件事由标题旁的徽章，以及「资料 > 关于本指南 > はじめに必ずお読みください」（5 条免责事项）承担。

阶段（phase）做成带标题条的块。底色用 `--ground`（与页面相同），其中的手续卡片用 `--surface`。如果底色用 `--surface-2`，浅色主题下卡片会浮起来，但深色主题下底色反而更亮、关系就颠倒了，所以采用现在这个组合。

手续标签页的开头放了概览块，显示进度、**下一步要做的事**、以及各阶段的推进情况。「下一步要做的事」指的是第一个尚未完成、且前置条件已全部完成的手续，这样就不用从长长的列表里从上往下找。点击阶段格子会跳转到该阶段。

## 内容有重叠的文章

「📅 年度日程（燕园校区／本部）」与「📅 新生留学日程」覆盖的范围有重叠。重叠的项目有：宿舍预约、签证申请、机票、报到、选课登记、期末考试、结业手续。

由于职责不同，没有合并。年度＝每年重复的学年历，新生＝从申请到结业只走一次的流程。但读者不知道该看哪一个，因此用 `EDITOR_NOTES` 在每篇文章开头给出引导（不是原文，所以用虚线框区别开）。手续的携带物品和前后关系由「手续」标签页承担，因此新生日程的内容与它也有重叠。

如果要在原文侧整理，最自然的做法是从新生日程中去掉年度的学期安排，换成指向年度日程的链接。

## 资料侧需要确认的点

- **住宿登记系统的 URL 在不同文章里不一致**（[2.1 抵达后](https://note.com/pku_guide_jp/n/n690d781e6106) 与 [2.2 住处](https://note.com/pku_guide_jp/n/n0dede70ff906)）。两者都是北京市公安局的系统，但现行是哪一个未确认。已记入 `zhusu_dengji` 的 `discrepancy`。
- **X1/X2 并不是资料之间的矛盾**。[签证](https://note.com/pku_guide_jp/n/nad1c4d69e689) 与 [居留许可](https://note.com/pku_guide_jp/n/ne3f008f7c5fd) 两篇都明写「X2 无需申请居留许可」。只是 [新生日程](https://note.com/pku_guide_jp/n/n960d0ae94257) 里「X1/X2→居留许可的切换」这个括注容易造成误读。已记入 `residence_permit` 的 `clarification`。最好在原文侧修正表述。

## 未收录・未实现

- PDF / DOC 附件 10 个（学费标准、保险说明、居留许可的申请书模板等）只给出了名称、大小和下载链接。**内容没有读取**，因此不得根据文件名推测内容来回答。参见 `materials/unread_attachments.json`。
- 图片有 93 处只保留了图所在的位置和说明文字（图片本体在 note 的外部主机上，Artifact 的 CSP 读不到）。地图、流程图、截图里的文字无法读取。一起分发的只有 5 张（年度日程、X1 / X2 签证与居留许可的样张、X1・X2 与居留许可的关系），需要其他图时由作者侧加入 `IMAGE_EMBED` 并放置 webp。已分发的图在 `alt` 里写清了内容，`docs.json` 中也会带上这段文字，因此模型也能"读到"图的内容。
- **问答的质量没有测量。** 检索侧是 12 道题 recall@8 = 12/12，但答案的正确性、引用的妥当性、以及该说「不知道」的场合能否说出来，都未评估。公开前应通过 `MATERIALS_REVIEW.md` 的验收标准（含资料缺失、条件不明、来源冲突、范围外的题目）。
- **`index.mjs`（Node 版）没有实际运行过。** 此环境没有 Node，只做到了语法的目视确认。上到 FC 后用 `curl <URL>/health` 确认。
- 术语表无法从正文自动抽取，因此手写 36 条。有系统性对照表的只有银行那篇文章（14 条）。
- 只在目录中出现、找不到正文的章节（公共交通 3.4 系、中文学习项目 6.1.2、申请材料 10 系）。详见 `MATERIALS_REVIEW.md`。
